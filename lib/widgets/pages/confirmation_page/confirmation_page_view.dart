import 'package:flutter/material.dart';

class ConformationPageView extends StatelessWidget {
  const ConformationPageView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
